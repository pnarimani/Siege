namespace Siege.Gameplay.UI
{
    public enum UILayer
    {
        World = 0,
        Screen = 100,
        Window = 200,
        Popup = 300,
        Dragging = 500,
        Overlay = 600,
        Tooltip = 700,
    }
}